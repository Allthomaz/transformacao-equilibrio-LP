---
import { Image } from 'astro:assets';
import retrato from '../assets/imagens/retrato-psicologa.jpg';
const WHATSAPP = 'https://wa.me/5511961205743';
const DOCTORALIA = 'https://www.doctoralia.com.br/andrea-rosa-souza/psicologo/sao-paulo';
---
<section class="hero" id="topo">
  <div class="hero__texto">
    <p class="kicker">Psicóloga clínica · Online e presencial · Tatuapé</p>
    <h1 class="hero__titulo">Você não precisa mais carregar isso sozinh<span class="hero__letra" aria-hidden="true">o</span><span class="visually-hidden">o, a ou x</span></h1>
    <p class="hero__sub">Se as noites viraram revirar na cama, os dias viraram piloto automático e a frase "vou resolver isso depois" já dura meses — existe um lugar pra destravar isso com calma. Atendimento presencial no Tatuapé ou online, com escuta e embasamento técnico. Adultos, jovens a partir de 14 anos e casais.</p>
    <div class="hero__ctas">
      <a class="btn" href={WHATSAPP} target="_blank" rel="noopener">Falar com a psicóloga</a>
      <a class="btn btn--outline" href={DOCTORALIA} target="_blank" rel="noopener">Verificar horários</a>
    </div>
    <p class="hero__micro">Sem compromisso · Respondo pessoalmente</p>
  </div>
  <div class="hero__foto">
    <Image src={retrato} alt="Andréa Rosa Souza, psicóloga clínica" width={620} height={620} />
  </div>
</section>
<style>
.hero { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1.1fr 0.9fr; align-items: center; gap: 5vw; padding: 8vh 5vw 10vh; }
.kicker { display: inline-flex; align-items: center; gap: 12px; font-size: 13px; font-weight: 500; letter-spacing: 3px; text-transform: uppercase; color: var(--terracota); margin-bottom: 26px; }
.kicker::before { content: ''; display: block; width: 34px; height: 1.5px; background: var(--terracota); }
.hero__titulo { font-family: var(--serif); font-weight: 400; font-size: clamp(40px, 5vw, 68px); line-height: 1.08; margin-bottom: 22px; }
.hero__letra { color: var(--terracota); font-style: italic; display: inline-block; min-width: .55em; transition: opacity .18s ease, transform .18s ease; }
.hero__letra--saindo { opacity: 0; transform: translateY(6px); }
.hero__sub { font-size: 17px; font-weight: 300; line-height: 1.65; color: var(--texto-suave); max-width: 480px; margin-bottom: 34px; }
.hero__ctas { display: flex; flex-wrap: wrap; gap: 14px; margin-bottom: 16px; }
.hero__micro { font-size: 13px; color: var(--texto-suave); }
.hero__foto img { border-radius: 24px; box-shadow: 0 24px 60px rgba(26,26,26,0.12); }
.visually-hidden { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; border: 0; }
@media (max-width: 900px) { .hero { grid-template-columns: 1fr; } }
</style>
<script>
  const letra = document.querySelector('.hero__letra');
  if (letra && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const ciclo = ['o', 'a', 'x'];
    let i = 0;
    setInterval(() => {
      i = (i + 1) % ciclo.length;
      letra.classList.add('hero__letra--saindo');
      setTimeout(() => {
        letra.textContent = ciclo[i];
        letra.classList.remove('hero__letra--saindo');
      }, 180);
    }, 2200);
  }
</script>
