# LP Andréa Rosa Souza — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Landing page de conversão (Astro 5) para a psicóloga Andréa Rosa Souza, com vídeo Remotion na seção "Como funciona", animações Motion One e deploy Vercel.

**Architecture:** Projeto Astro isolado em `lp/` com um componente por seção da LP e design tokens CSS compartilhados. Projeto Remotion separado em `video/` que renderiza MP4 para dentro de `lp/public/videos/`. Protótipos standalone existentes (`secao-acolhimento/`) servem de referência visual validada.

**Tech Stack:** Astro 5, CSS puro (tokens), Motion One, Remotion, Google Fonts (DM Serif Display + DM Sans), Vercel.

**Fonte de copy:** `docs/copy-otimizada.md` (copy tangível revisada) — usar como fonte primária; fallback `briefing-andrea.md` §8 e spec §4. **Antes de iniciar Task 3+, confirmar que `docs/copy-otimizada.md` existe**; se não existir, usar a copy da spec.

**Verificação visual:** usar Chrome DevTools MCP (`mcp__chrome-devtools__*`): `new_page` → `http://localhost:4321` → `take_screenshot` desktop (1440×900) e mobile (390×844 via `resize_page`).

---

### Task 1: Scaffold do projeto Astro

**Files:**
- Create: `lp/` (projeto Astro completo)
- Create: `lp/src/styles/tokens.css`
- Create: `lp/.nvmrc`

- [ ] **Step 1: Scaffold minimal**

```bash
cd /home/flow/TM-System/Condado/psicologa
npm create astro@latest lp -- --template minimal --no-install --no-git --typescript strict --yes
cd lp && npm install
echo "22" > .nvmrc
```

- [ ] **Step 2: tokens.css com a paleta e tipografia**

Criar `lp/src/styles/tokens.css`:

```css
:root {
  --bg: #F5F1EA;
  --bege: #E8DCC0;
  --texto: #1A1A1A;
  --terracota: #B25F3F;
  --terracota-escuro: #96492E;
  --verde: #4A7C59;
  --superficie: #FBF8F2;
  --borda: rgba(26, 26, 26, 0.08);
  --texto-suave: rgba(26, 26, 26, 0.6);
  --serif: 'DM Serif Display', Georgia, serif;
  --sans: 'DM Sans', system-ui, sans-serif;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  font-family: var(--sans);
  background: var(--bg);
  color: var(--texto);
  -webkit-font-smoothing: antialiased;
}
img, video { max-width: 100%; display: block; }
```

- [ ] **Step 3: layout base com fontes**

`lp/src/layouts/Layout.astro` (substituir o default):

```astro
---
import '../styles/tokens.css';
interface Props { title: string; description: string; }
const { title, description } = Astro.props;
---
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="icon" type="image/png" href="/favicon.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,400&display=swap" rel="stylesheet" />
  <slot name="head" />
</head>
<body><slot /></body>
</html>
```

- [ ] **Step 4: Copiar assets estáticos**

```bash
cd /home/flow/TM-System/Condado/psicologa
mkdir -p lp/public/videos lp/src/assets/imagens
cp assets/imagens/retrato-psicologa-criativo.jpg assets/imagens/foto-da-psicologa.jpeg \
   assets/imagens/consultorio.jpeg assets/imagens/criativas/consultorio-criativo-sofa.jpg \
   assets/imagens/criativas/consultorio-criativo-sala.jpg lp/src/assets/imagens/
cp assets/imagens/logo-fundo-branco-fivicon.png lp/public/favicon.png
cp assets/imagens/logo-fundo-criativo-02.png lp/src/assets/imagens/
```

- [ ] **Step 5: Build de sanidade**

Run: `cd lp && npm run build`
Expected: `Complete!` sem erros.

- [ ] **Step 6: Commit**

```bash
git add lp .gitignore && git commit -m "feat(lp): scaffold Astro + design tokens + assets"
```

---

### Task 2: Header + Hero com headline animada o/a/x

**Files:**
- Create: `lp/src/components/Header.astro`
- Create: `lp/src/components/Hero.astro`
- Modify: `lp/src/pages/index.astro`

- [ ] **Step 1: Header.astro**

```astro
---
import logo from '../assets/imagens/logo-fundo-criativo-02.png';
import { Image } from 'astro:assets';
const WHATSAPP = 'https://wa.me/5511961205743';
---
<header class="header">
  <div class="header__inner">
    <a href="#topo" class="header__logo" aria-label="Transformação & Equilíbrio — início">
      <Image src={logo} alt="Logo Transformação & Equilíbrio" width={180} height={60} />
    </a>
    <nav class="header__nav" aria-label="Principal">
      <a href="#atendimento">Atendimento</a>
      <a href="#servicos">Serviços</a>
      <a href="#sobre">Sobre</a>
      <a href="#faq">FAQ</a>
    </nav>
    <a class="btn btn--header" href={WHATSAPP} target="_blank" rel="noopener">Agendar conversa</a>
  </div>
</header>
<style>
.header { position: sticky; top: 0; z-index: 50; background: rgba(245,241,234,0.92); backdrop-filter: blur(12px); border-bottom: 1px solid var(--borda); }
.header__inner { max-width: 1200px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between; gap: 24px; padding: 14px 5vw; }
.header__logo img { height: 52px; width: auto; }
.header__nav { display: flex; gap: 28px; }
.header__nav a { font-size: 14px; font-weight: 400; color: var(--texto-suave); text-decoration: none; }
.header__nav a:hover { color: var(--terracota); }
@media (max-width: 760px) { .header__nav { display: none; } }
</style>
```

CSS global dos botões — adicionar ao final de `tokens.css`:

```css
.btn {
  display: inline-flex; align-items: center; gap: 10px;
  padding: 14px 28px; border-radius: 999px;
  font-family: var(--sans); font-size: 15px; font-weight: 500; text-decoration: none;
  background: var(--terracota); color: #fff;
  box-shadow: 0 8px 24px rgba(178,95,63,0.35);
  transition: background .2s ease, transform .2s ease;
}
.btn:hover { background: var(--terracota-escuro); transform: translateY(-2px); }
.btn--outline { background: transparent; color: var(--terracota); border: 1.5px solid var(--terracota); box-shadow: none; }
.btn--outline:hover { background: rgba(178,95,63,0.08); }
.btn--header { padding: 10px 20px; font-size: 14px; }
```

- [ ] **Step 2: Hero.astro com headline o→a→x**

```astro
---
import { Image } from 'astro:assets';
import retrato from '../assets/imagens/retrato-psicologa-criativo.jpg';
const WHATSAPP = 'https://wa.me/5511961205743';
const DOCTORALIA = 'https://www.doctoralia.com.br/andrea-rosa-souza/psicologo/sao-paulo';
// headline: usar docs/copy-otimizada.md §1 se existir; fallback abaixo
---
<section class="hero" id="topo">
  <div class="hero__texto">
    <span class="kicker">Psicóloga clínica · Online e presencial · Tatuapé</span>
    <h1 class="hero__titulo">
      Você não precisa mais carregar isso sozinh<span class="hero__letra" aria-hidden="true">o</span><span class="visually-hidden">o, a ou x</span>
    </h1>
    <p class="hero__sub">Atendimento acolhedor e com embasamento técnico, presencial no Tatuapé ou online. Adultos e jovens a partir de 14 anos; casais.</p>
    <div class="hero__ctas">
      <a class="btn" href={WHATSAPP} target="_blank" rel="noopener">Falar com a psicóloga</a>
      <a class="btn btn--outline" href={DOCTORALIA} target="_blank" rel="noopener">Verificar horários</a>
    </div>
    <p class="hero__micro">Sem compromisso · Respondo pessoalmente</p>
  </div>
  <div class="hero__foto">
    <Image src={retrato} alt="Andréa Rosa Souza, psicóloga clínica" width={620} height={620} />
  </div>
</section>
<style>
.hero { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1.1fr 0.9fr; align-items: center; gap: 5vw; padding: 8vh 5vw 10vh; }
.kicker { display: inline-flex; align-items: center; gap: 12px; font-size: 13px; font-weight: 500; letter-spacing: 3px; text-transform: uppercase; color: var(--terracota); margin-bottom: 26px; }
.kicker::before { content: ''; width: 34px; height: 1.5px; background: var(--terracota); }
.hero__titulo { font-family: var(--serif); font-weight: 400; font-size: clamp(40px, 5vw, 68px); line-height: 1.08; margin-bottom: 22px; }
.hero__letra { color: var(--terracota); font-style: italic; display: inline-block; min-width: 0.55em; text-align: left; animation: troca-letra 6s step-end infinite; }
.hero__sub { font-size: 17px; font-weight: 300; line-height: 1.65; color: var(--texto-suave); max-width: 480px; margin-bottom: 34px; }
.hero__ctas { display: flex; flex-wrap: wrap; gap: 14px; margin-bottom: 16px; }
.hero__micro { font-size: 13px; color: var(--texto-suave); }
.hero__foto img { border-radius: 24px; box-shadow: 0 24px 60px rgba(26,26,26,0.12); }
.visually-hidden { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); }
@media (prefers-reduced-motion: reduce) { .hero__letra { animation: none; } }
@media (max-width: 900px) { .hero { grid-template-columns: 1fr; padding-top: 6vh; } }
@keyframes troca-letra {
  0%, 32% { opacity: 1; } 33%, 65% { content: 'a'; } 66%, 100% { content: 'x'; }
}
</style>
<script>
  // keyframes com content: não troca texto de forma confiável — usar JS leve e acessível
  const letra = document.querySelector('.hero__letra');
  if (letra && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const ciclo = ['o', 'a', 'x'];
    let i = 0;
    setInterval(() => {
      i = (i + 1) % ciclo.length;
      letra.classList.add('hero__letra--saindo');
      setTimeout(() => {
        letra.textContent = ciclo[i];
        letra.classList.remove('hero__letra--saindo');
      }, 180);
    }, 2200);
  }
</script>
```

Adicionar ao `<style>` do Hero: `.hero__letra--saindo { opacity: 0; transform: translateY(6px); }` e na `.hero__letra`: `transition: opacity .18s ease, transform .18s ease;` (a keyframe CSS acima pode ser removida — o JS é a implementação real).

- [ ] **Step 3: index.astro montando Header + Hero**

```astro
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
---
<Layout title="Andréa Rosa Souza — Psicóloga no Tatuapé & Online | Transformação & Equilíbrio" description="Psicoterapia acolhedora com embasamento técnico. Adultos, jovens e casais. Presencial no Tatuapé ou online. CRP 06/136303.">
  <Header />
  <main>
    <Hero />
  </main>
</Layout>
```

- [ ] **Step 4: Verificação visual**

Run: `cd lp && npm run dev` → Chrome DevTools: `new_page http://localhost:4321`, screenshot 1440×900 e 390×844.
Expected: header sticky com logo, headline com letra ciclando o→a→x em terracota, CTAs ok, acentuação correta.

- [ ] **Step 5: Commit**

```bash
git add lp && git commit -m "feat(lp): header + hero com headline animada o/a/x"
```

---

### Task 3: Marquee loop

**Files:**
- Create: `lp/src/components/Marquee.astro`
- Modify: `lp/src/pages/index.astro` (import + `<Marquee />` após `<Hero />`)

- [ ] **Step 1: Marquee.astro**

```astro
---
// palavras: docs/copy-otimizada.md §8 se existir; fallback abaixo
const palavras = ['EQUILÍBRIO','ESCUTA','ACOLHIMENTO','TRANSFORMAÇÃO','RESPEITO AO RITMO','SEM JUÍZO','PRESENCIAL','ONLINE','TATUAPÉ'];
const faixa = [...palavras, ...palavras]; // duplicado p/ loop seamless
---
<div class="marquee" role="presentation" aria-hidden="true">
  <div class="marquee__track">
    {faixa.map((p, i) => (
      <span class={`marquee__palavra ${i % 2 ? 'marquee__palavra--destaque' : ''}`}>
        {p}<span class="marquee__ponto"></span>
      </span>
    ))}
  </div>
</div>
<style>
.marquee { overflow: hidden; border-top: 1px solid var(--borda); border-bottom: 1px solid var(--borda); padding: 14px 0; background: var(--superficie); }
.marquee__track { display: flex; width: max-content; animation: marquee 40s linear infinite; }
.marquee__palavra { display: inline-flex; align-items: center; padding: 0 32px; font-size: 0.7rem; letter-spacing: 0.18em; font-weight: 500; color: var(--texto-suave); flex-shrink: 0; }
.marquee__palavra--destaque { color: var(--terracota); }
.marquee__ponto { display: inline-block; width: 3px; height: 3px; border-radius: 50%; background: var(--borda); margin-left: 32px; }
@keyframes marquee { from { transform: translateX(0); } to { transform: translateX(-50%); } }
@media (prefers-reduced-motion: reduce) { .marquee__track { animation: none; } }
</style>
```

- [ ] **Step 2: Verificar loop seamless no browser** (o reset a 0 é invisível porque a segunda metade é idêntica à primeira)

- [ ] **Step 3: Commit** — `git commit -m "feat(lp): marquee loop de palavras"`

---

### Task 4: Prova social + Sobre

**Files:**
- Create: `lp/src/components/ProvaSocial.astro`
- Create: `lp/src/components/Sobre.astro`
- Modify: `lp/src/pages/index.astro`

- [ ] **Step 1: ProvaSocial.astro** — nota 5,0/70 avaliações, selos 2019/2020, 3 trechos verificados (copiar do `briefing-andrea.md` §3). Grid: card de nota à esquerda (número gigante DM Serif `5,0`, estrelas em SVG inline, "70 avaliações no Doctoralia", badges dos selos) + 3 blockquotes à direita com aspas serifas.

- [ ] **Step 2: Sobre.astro** — texto aprovado (spec §4 item 8, briefing §8), foto `foto-da-psicologa.jpeg` se destaque do retrato já usado no hero, badges linha inferior: "+10 anos de clínica · CRP 06/136303 · Rua Apucarana, Tatuapé". Formações em lista discreta (UNIP, Grupo Wainer, Instituto Freedom, UNIFESP).

- [ ] **Step 3: Verificação visual + acentuação** (ç, ã, é — checar renderização)

- [ ] **Step 4: Commit** — `git commit -m "feat(lp): prova social + sobre"`

---

### Task 5: Atendimento — cards premium com shine sweep

**Files:**
- Create: `lp/src/components/Atendimento.astro`
- Modify: `lp/src/pages/index.astro`

- [ ] **Step 1: Atendimento.astro**

Estrutura: kicker "Atendimento" → título DM Serif "Tratamento e acolhimento a questões como:" → subtítulo → grid `repeat(auto-fit, minmax(260px, 1fr))` gap 18px → CTA final. Cards (copy de `docs/copy-otimizada.md` §3; fallback spec §4 item 5): cada card `<a href={WHATSAPP}>` com título em DM Serif 22px + frase DM Sans 300 14px.

CSS do shine (o coração do premium):

```css
.card {
  position: relative; overflow: hidden;
  background: var(--superficie);
  border: 1px solid var(--borda);
  border-radius: 20px;
  padding: 28px 24px;
  text-decoration: none; color: var(--texto);
  transition: transform .3s cubic-bezier(0.22, 1, 0.36, 1), box-shadow .3s ease;
}
.card::before {
  content: ''; position: absolute; inset: -60% auto; top: 0; bottom: 0; left: -80%;
  width: 55%;
  background: linear-gradient(115deg, transparent, rgba(255,255,255,0.55), transparent);
  transform: skewX(-20deg) translateX(-140%);
  transition: none;
}
.card:hover { transform: translateY(-3px); box-shadow: 0 18px 40px rgba(26,26,26,0.08); }
.card:hover::before, .card:active::before { animation: shine 0.9s cubic-bezier(0.22, 1, 0.36, 1) forwards; }
@keyframes shine { to { transform: skewX(-20deg) translateX(340%); } }
@media (prefers-reduced-motion: reduce) { .card:hover { transform: none; } .card::before { display: none; } }
```

- [ ] **Step 2: Verificar shine visível** — screenshot com hover simulado (`hover` tool do Chrome DevTools num card) e conferir que a faixa atravessa; se invisível, subir `rgba(255,255,255,0.55)` → `0.7`.

- [ ] **Step 3: Commit** — `git commit -m "feat(lp): seção atendimento com cards shine sweep"`

---

### Task 6: Serviços + Acolhimento + FAQ + CTA final + Rodapé

**Files:**
- Create: `lp/src/components/Servicos.astro`, `lp/src/components/Acolhimento.astro`, `lp/src/components/Faq.astro`, `lp/src/components/CtaFinal.astro`, `lp/src/components/Rodape.astro`
- Modify: `lp/src/pages/index.astro`

- [ ] **Step 1: Servicos.astro** — 4 cards (spec §4 item 6): Psicoterapia Individual (badge Online + Presencial · Tatuapé; copy interna cita ansiedade, depressão, fobias, traumas, TOC, bipolar, borderline), Terapia de Casal, Avaliação p/ Bariátrica com laudo, Avaliação Psicológica. Sem preços. Reaproveitar `.card` do Atendimento via classe global (extrair `.card` para `tokens.css` se necessário — preferir duplicar escopado no componente).

- [ ] **Step 2: Acolhimento.astro** — base no `secao-acolhimento/index.html` aprovado, MAS texto reposicionado: foto `consultorio.jpeg` full-bleed (92vh) com frase em DM Serif clamp(32px, 4vw, 56px) alinhada à esquerda-baixo em container `max-width: 520px` com `padding: 0 8vw 8vh` e leve gradiente inferior na foto (`linear-gradient(transparent 40%, rgba(26,26,26,0.55))`) para leitura sem cobrir o centro da imagem.

- [ ] **Step 3: Faq.astro** — `<details>` nativos estilizados: particular sem convênios / formas de pagamento (PIX, crédito, débito, transferência, boleto) + recibo p/ reembolso / como funciona o online / atende jovens a partir de 14 anos / valores: consultar diretamente.

- [ ] **Step 4: CtaFinal.astro** — headline de baixa pressão (copy otimizada §7), CTA duplo WhatsApp + Doctoralia, microcopy.

- [ ] **Step 5: Rodape.astro** — logo, CRP 06/136303, Rua Apucarana — Tatuapé/SP, CNPJ 36.940.050/0001-04, links WhatsApp/Doctoralia, "© 2026 Transformação & Equilíbrio".

- [ ] **Step 6: Ordem final no index.astro**

Hero → Marquee → ProvaSocial → Atendimento → Servicos → Acolhimento → Sobre → ComoFunciona (Task 8 placeholder `<div id="como-funciona" />` por enquanto) → Faq → CtaFinal → Rodapé

- [ ] **Step 7: Verificação visual full-page** (`take_screenshot` com `fullPage: true`)

- [ ] **Step 8: Commit** — `git commit -m "feat(lp): serviços, acolhimento, faq, cta final e rodapé"`

---

### Task 7: Motion One — coreografia de entrada

**Files:**
- Create: `lp/src/scripts/animar.ts`
- Modify: `lp/src/layouts/Layout.astro`

- [ ] **Step 1: Instalar**

```bash
cd lp && npm install motion
```

- [ ] **Step 2: animar.ts**

```ts
import { animate, inView, stagger } from 'motion';

const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
if (!reduced) {
  // Hero: entrada em cascata
  animate('.hero__texto > *',
    { opacity: [0, 1], transform: ['translateY(18px)', 'translateY(0px)'] },
    { duration: 0.7, delay: stagger(0.12), easing: [0.22, 1, 0.36, 1] });

  // Reveals on-scroll: qualquer elemento com [data-reveal]
  inView('[data-reveal]', (info) => {
    animate(info.target,
      { opacity: [0, 1], transform: ['translateY(24px)', 'translateY(0px)'] },
      { duration: 0.6, easing: [0.22, 1, 0.36, 1] });
    info.target.removeAttribute('data-reveal');
  }, { margin: '-15% 0px' });
}
```

Adicionar `data-reveal` + `opacity: 0` inicial (via classe `.reveal-inicial` aplicada só quando JS roda — evitar conteúdo invisível sem JS) nos títulos/grades de ProvaSocial, Atendimento, Servicos, Sobre, Faq, CtaFinal.

- [ ] **Step 3: Carregar no Layout** — `<script src="../scripts/animar.ts"></script>` antes de `</body>`.

- [ ] **Step 4: Verificar** — reload da página no browser: entradas em cascata no hero, reveals ao rolar; com DevTools emulating `prefers-reduced-motion: reduce`, página estática.

- [ ] **Step 5: Commit** — `git commit -m "feat(lp): animações Motion One (entrada hero + reveals)"`

---

### Task 8: Vídeo Remotion "Como funciona"

**Files:**
- Create: `video/` (projeto Remotion)
- Create: `lp/src/components/ComoFunciona.astro`
- Modify: `lp/src/pages/index.astro`

**OBRIGATÓRIO: invocar a skill `remotion-motion-graphics` antes de escrever qualquer código Remotion deste task.**

- [ ] **Step 1: Scaffold Remotion**

```bash
cd /home/flow/TM-System/Condado/psicologa
npx create-video@latest video --blank
cd video && npm install
```

- [ ] **Step 2: Composição** (`video/src/ComoFunciona.tsx`) — 1920×1080, 30fps, ~12s, três cenas (uma por passo: contato → 1ª sessão → acompanhamento), tema do skill adaptado à paleta (#F5F1EA fundo, #B25F3F destaque, DM Serif Display via `@remotion/google-fonts`). Seguir TODAS as regras da skill: spring(), sem linear, stagger 3-6 frames, exits mais rápidos, 5-layer stack (mesh de fundo + tipografia + grade de cor + grain/vignette), respiração sin em elementos parados. Copy dos passos de `docs/copy-otimizada.md` §5.

- [ ] **Step 3: Renderizar**

```bash
cd video && npx remotion render ComoFunciona ../lp/public/videos/como-funciona.mp4 --codec=h264 --crf=20
```

Verificar tamanho < 2MB; se maior, `--crf=24` e reduzir para 1280×720.

- [ ] **Step 4: ComoFunciona.astro** — título + os 3 passos em texto (acessível/SEO, sempre visíveis) + `<video>` abaixo:

```astro
<section class="como" id="como-funciona">
  <!-- título + 3 passos texto (copy otimizada §5) -->
  <video class="como__video" muted loop playsinline preload="none"
    poster={poster} data-video-scroll src="/videos/como-funciona.mp4"></video>
</section>
<script>
  const v = document.querySelector('[data-video-scroll]');
  if (v) {
    const obs = new IntersectionObserver(([e]) => {
      if (e.isIntersecting) v.play().catch(() => {}); else v.pause();
    }, { threshold: 0.4 });
    obs.observe(v);
  }
</script>
```

Poster: frame extraído do vídeo (`npx remotion still` no frame 30 → `como-funciona-poster.jpg`).

- [ ] **Step 5: Verificar** — scroll até a seção, vídeo dá play; Lighthouse mobile ≥ 85 ( vídeo com preload none).

- [ ] **Step 6: Commit** — `git commit -m "feat(lp): vídeo Remotion na seção como funciona"`

---

### Task 9: SEO + JSON-LD + og

**Files:**
- Modify: `lp/src/layouts/Layout.astro`
- Create: `lp/src/pages/_sitemap` ou `@astrojs/sitemap`

- [ ] **Step 1: JSON-LD** no Layout (`<slot name="head" />`):

```json
{
  "@context": "https://schema.org",
  "@type": "Psychologist",
  "name": "Andréa Rosa Souza",
  "alternateName": "Transformação & Equilíbrio",
  "url": "https://DOMINIO",
  "telephone": "+5511961205743",
  "address": { "@type": "PostalAddress", "streetAddress": "Rua Apucarana", "addressLocality": "Tatuapé, São Paulo", "addressCountry": "BR" },
  "identifier": "CRP 06/136303",
  "priceRange": "$$"
}
```

(DOMINIO será trocado na Task 10 quando o cliente confirmar.)

- [ ] **Step 2: og:image + twitter card** — usar `logo-com-fundo-criativo.png` como og provisório; `@astrojs/sitemap` com `site` no `astro.config`.

- [ ] **Step 3: Commit** — `git commit -m "feat(lp): SEO, JSON-LD e sitemap"`

---

### Task 10: Deploy Vercel

**Files:**
- Create: `lp/vercel.json` (se necessário)
- Modify: `lp/astro.config.mjs` (`site` com o domínio dela)

- [ ] **Step 1:** `npm run build` local verde.
- [ ] **Step 2:** `npx vercel` no diretório `lp/` (preview), validar URL de preview no browser mobile + desktop.
- [ ] **Step 3:** Produção + orientar migração de DNS: no painel do domínio atual dela, trocar A/CNAME para os do Vercel; esperar propagação; desligar o site antigo só depois do novo no ar.
- [ ] **Step 4:** Commit final + tag `v1.0.0-lp`.

---

## Self-Review (executado)

- **Cobertura da spec:** header(2) hero(2) marquee(3) prova social(4) atendimento(5) serviços(6) acolhimento(6) sobre(4) como funciona/vídeo(8) FAQ(6) CTA final(6) rodapé(6) Motion One(7) SEO(9) deploy(10) ✅
- **Sem placeholders:** copy aponta sempre para fonte concreta com fallback escrito ✅
- **Consistência:** `WHATSAPP`/`DOCTORALIA` constantes idênticas em Tasks 2/5/6 ✅; `.card` definido no Task 5 e reaproveitado no 6 com nota de escopo ✅
